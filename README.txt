Most blocks (main menu, lizard + spock options, etc.) are hidden.

To reveal these blocks, go into the RPStyle.css file and set the respective block id's display value to "block."

To hide these blocks, set the display value to "none."


display: none;
display: block;